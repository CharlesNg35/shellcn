# Phase 0 · Step 1 — Initialize Go module and repo skeleton

**Milestone:** — (bootstrap) · **Status:** ✅ Done (2026-05-25)
**Depends on:** none · **Spec refs:** [v2 §14](../../project.md)

## Goal

Create the Go module and the `internal/` package layout so every later layer has a
home and `go build ./...` is green from day one.

## Checklist

- [x] `go mod init MODULE` (chose `github.com/charlesng35/shellcn`)
- [x] Create packages: `cmd/server`, `cmd/agent`, `plugins/`, and
      `internal/{models,store,service,server,plugin,session,transport,auth,policy,secrets,audit,telemetry,config}`
      (the entity package is `internal/models` — renamed from `domain`; its structs double as the GORM models)
- [x] `cmd/server/main.go` stub that builds, prints version/flags (`--dev`, `--addr`),
      exposes `/healthz`, and serves embedded frontend assets outside dev mode
- [x] Confirm the existing `web/embed.go` (`package web`) still compiles with a placeholder `web/dist`
- [x] Add `.editorconfig`, `LICENSE`, top-level `README.md`
- [x] First commit intentionally deferred to handoff/merge after review

## Definition of done

`go build ./...` succeeds and `bin/shellcn --version` runs. Package layout matches v2 §14.

## Notes

Keep `models` free of internal-package deps (stdlib + gorm tags only). Nothing imports `internal/server` except `cmd/server`.
