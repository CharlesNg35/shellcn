# Phase 0 · Step 3 — Makefile and developer tooling

**Milestone:** — (bootstrap) · **Status:** ✅ Done (2026-05-25)
**Depends on:** 0.1, 0.2 · **Spec refs:** [v2 §14.3](../../project.md), [v2 §11](../../project.md)

## Goal

One-command build/dev/test/lint so DX is good from the first commit.

## Checklist

- [x] `Makefile` targets: `build` (vite build → `go build -o bin/shellcn ./cmd/server`),
      `dev-web` (Vite), `dev-api` (`go run ./cmd/server --dev`, Vite proxies `/api`),
      `test`, `lint`
- [x] `golangci-lint` config (v2) + `gofumpt` (formatter via `golangci-lint fmt`)
- [x] Go live-reload (`wgo`) for `dev-api`
- [x] Frontend ESLint + Prettier; TS strict
- [x] No codegen — data layer is GORM + `AutoMigrate` (no `go generate`)
- [x] CI workflow stub: build + lint + test (`.github/workflows/ci.yml`)

## Definition of done

`make build` produces a single binary that serves the embedded frontend; `make lint` and `make test` run clean.

## Notes

`--dev` mode should serve the API only and let Vite serve the UI (proxy `/api` → Go); production serves embedded `web/dist`.
