# Phase 2 · Step 1 — Package skeleton and plugin contract types

**Milestone:** M1 · **Status:** ✅ Done (2026-05-25)
**Depends on:** 0.1 · **Spec refs:** [v2 §5](../../project.md), [v2 §6](../../project.md), [v2 §8](../../project.md)

## Goal
Define the Go plugin contract in `internal/plugin`, matching `projection.ts` (1.1)
so FE and BE agree on shape.

## Checklist
- [x] `Plugin` (Manifest, Routes, Connect), `Session`/`Channel`/`ClientStream`
- [x] `Manifest` (+ `Capability`, `Transport`, `AgentMode`, `AgentProfile`, `ProxyTarget`, `InstallArtifact`)
- [x] `Icon` (+ `IconType` lucide/url/base64/emoji/svg) — used by Manifest/Tab/TreeGroup/Action/Resource icon fields (§5.1)
- [x] `Route` (+ `Method`, `RiskLevel`), `Handler`, `StreamHandler`
- [x] `RequestContext` (`Param`, `Query`, `Bind`, `Page`, declared-schema validation, multipart uploads) — typed, no `map[string]any` assertions
- [x] `ConnectConfig` + `NetTransport` (`DialContext` L4, `HTTP()` L7)
- [x] `Schema`/`Field`/`Condition`, `Tab`/`TreeGroup`/`DataSource`, `ResourceType`/`Action`/`Stream`/`ResourceRef`
- [x] `PanelType` constants incl. the extensible set (`graph`/`trace`/`kv`/`http_client`) (§6.2)
- [x] `registry` (Register/Get/All)

## Definition of done
Types compile; a golden test round-trips a manifest to the exact JSON shape `projection.ts` expects.

## Tests (gate)
- [x] Golden: `Manifest` (with structured `Icon`) JSON-marshals to the `projection.ts` shape (1.1) — FE/BE drift guard
- [x] Unit: `RequestContext.Bind` decodes + validates typed structs (e.g. JSON number → int) with **no panic** on missing/wrong type
- [x] Unit: `RequestContext.ValidateSchema` enforces declared input fields, validators, visibility, options, and multipart form values before handlers run
- [x] `go vet` + `-race` clean

## Notes
Plugin structs hold **no** per-connection state (§5). Per-connection state lives in `Session`. See [`../TESTING.md`](../TESTING.md).
