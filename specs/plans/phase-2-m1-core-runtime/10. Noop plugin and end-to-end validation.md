# Phase 2 · Step 10 — Test plugin and end-to-end validation

**Milestone:** M1 · **Status:** ✅ Done (2026-05-25)
**Depends on:** 2.2–2.9, 1.7 · **Spec refs:** [v2 §15 (M1)](../../project.md)

## Goal
A trivial test-only plugin exercises the whole core path, then point the M0 UI
at the real API (retire the mock server).

## Checklist
- [x] Internal test plugin: manifest + one GET list route (`Page<Row>`) + one WS echo stream
- [x] Register it only in server e2e tests
- [x] Switch the frontend from `VITE_API=mock` to `real`
- [x] Verify end-to-end: projection renders → table loads via resolver → WS echo through the wrapper → audit row written → authz enforced
- [x] Confirm the M0 UI is unchanged (only the API base differs)

## Definition of done
The M0 UI runs against the **real** server end-to-end with the test plugin; the mock server is no longer required.

## Phase 2 exit (M1)
Core runtime proven by a real (if trivial) plugin. Update [`../README.md`](../README.md) Phase 2 to ✅.
