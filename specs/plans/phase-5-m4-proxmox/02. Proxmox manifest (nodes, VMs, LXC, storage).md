# Phase 5 · Step 2 — Proxmox manifest (nodes, VMs, LXC, storage)

**Milestone:** M4 · **Status:** ☐ Not started
**Depends on:** 5.1 · **Spec refs:** [v2 §6.3](../../v2.md), [v2 §13](../../v2.md)

## Goal
Deep-hierarchy sidebar tree + tabbed VM/LXC detail views.

## Checklist
- [ ] `LayoutSidebarTree`; tree: Nodes / Storage / Network / Datacenter
- [ ] VM detail tabs: Overview (metrics), Console (`remote_desktop` with `engine: "novnc"`), Hardware (form), Snapshots (table), Backups (table)
- [ ] LXC detail tabs: Overview, Console (terminal), Config
- [ ] Actions: start/stop/reboot (`write`), snapshot (`write`), migrate (`write`), delete (`destructive`) — confirm on destructive
- [ ] Passes manifest validation

## Definition of done
Proxmox renders the tree and VM/LXC detail tabs from the projection, replacing the fixture.

## Notes
Proves the `DetailView`-with-tabs model on a genuinely deep hierarchy (v2 §13).
