# Phase 5 · Step 1 — Proxmox session and API client

**Milestone:** M4 · **Status:** ✅ Done (2026-05-26)
**Depends on:** Phase 2 · **Spec refs:** [v2 §8](../../project.md), [v2 §7.2](../../project.md)

## Goal
A session over the Proxmox VE REST API, with list routes for the cluster hierarchy.

## Checklist
- [ ] Auth: API token and/or ticket + CSRF
- [ ] Reach the PVE API via `cfg.Net` (direct now; agent-capable later)
- [ ] List routes (paginated): nodes, VMs (QEMU), LXC, storage, network
- [ ] Task-status route (for long-running operations)
- [ ] `HealthCheck` + `Close`

## Definition of done
List nodes / VMs / LXC / storage from a test Proxmox cluster.

## Notes
Proxmox is REST/JSON; this validates an HTTP-based plugin (vs. socket-based Docker/SSH).
