# Phase 2d · Step 1 — Backend typed bootstrap config

**Milestone:** M-Admin · **Status:** ✅ Done (2026-05-25)
**Depends on:** 2.4, 2.5 · **Spec refs:** [v2 §9.1](../../project.md), [v2 §12.2](../../project.md)

## Goal
Centralize bootstrap/runtime settings in typed config loaded from `config.yaml`,
environment variables, and flags.

## Checklist
- [x] Add `internal/config` typed config loader with defaults and validation
- [x] Support `config.yaml`, `SHELLCN_*` environment variables, and flag overrides
- [x] Unify master-key loading through config
- [x] Keep SMTP/email settings in config, not a mutable DB table
- [x] Add config tests for defaults, env/flag overrides, and invalid values

## Definition of done
The server starts from typed config with deterministic defaults, supports
operator overrides, and rejects invalid configuration before runtime use.

## Tests (gate)
- [x] Config unit tests cover defaults, overrides, and validation failures
