# Phase 4 · Step 1 — Docker session and resource routes

**Milestone:** M3 · **Status:** ✅ Done (2026-05-26)
**Depends on:** Phase 2 · **Spec refs:** [v2 §8.2](../../project.md), [v2 §7.2](../../project.md), [v2 §7.3](../../project.md)

## Goal
Docker session via `cfg.Net.DialContext` (so direct and agent share one path) plus
the resource list/inspect routes.

## Checklist
- [x] `dockerclient.NewClientWithOpts(WithDialContext(cfg.Net.DialContext))`
- [x] List routes (paginated): containers, images, volumes, networks
- [x] Inspect routes (per resource)
- [x] Watch route via Docker events → `ResourceEvent`
- [x] `HealthCheck` (ping daemon) + `Close`

## Definition of done
List and inspect Docker resources from a test daemon over **direct** transport.

## Notes
Treat Docker connections as `privileged` by default — socket access is root-equivalent (§9.6).
