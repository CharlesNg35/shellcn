# Phase 3 · Step 2 — SSH routes and manifest

**Milestone:** M2 · **Status:** ☐ Not started
**Depends on:** 3.1 · **Spec refs:** [v2 §5](../../v2.md), [v2 §6.2](../../v2.md), [v2 §13](../../v2.md)

## Goal
Declare SSH's manifest (tabs layout) and typed routes with correct risk levels.

## Checklist
- [ ] Manifest: `LayoutTabs`, tabs `Terminal / Files / Tunnels / Snippets`, config schema (host/port/auth/key with conditional fields)
- [ ] `ssh.shell` — WS, `privileged`
- [ ] `ssh.sftp.list/read/stat` — `safe`; `ssh.sftp.write/mkdir/rm/rename` — `write`
- [ ] `ssh.tunnel.open/close` — `privileged`
- [ ] Snippets routes (`safe`/`write`)
- [ ] Each route sets `Permission`, `Risk`, `AuditEvent`; passes manifest validation (2.2)

## Definition of done
The real SSH projection renders the SSH tabs in the UI (replacing the SSH fixture), driven entirely by the manifest.

## Notes
Replace `web/fixtures/ssh.json` consumption with the live projection for this connection.
