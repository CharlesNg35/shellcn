# Phase 3 · Step 2 — SSH routes and manifest

**Milestone:** M2 · **Status:** ✅ Done (2026-05-25)
**Depends on:** 3.1 · **Spec refs:** [v2 §5](../../v2.md), [v2 §6.2](../../v2.md), [v2 §13](../../v2.md)

## Goal
Declare SSH's manifest (tabs layout) and typed routes with correct risk levels.

## Checklist
- [x] Manifest: `LayoutTabs`, tabs `Terminal / Files / Snippets`, config schema (host/port/auth/key with conditional fields)
- [x] Manifest declares SSH-owned reusable credential kinds (`ssh_private_key`, `ssh_password`); SFTP consumes those kinds through its own selector, and registry-derived compatibility adds both `ssh` and `sftp`
- [x] `ssh.shell` — WS, `privileged`
- [x] `ssh.sftp.list/read/download/stat` — `safe`; `ssh.sftp.write/upload/mkdir/delete/rename` — `write`
- [x] Files tab config declares the generic M0 route IDs: `readRouteId`, `downloadRouteId`, `writeRouteId`, `uploadRouteId`, `mkdirRouteId`, `renameRouteId`, `deleteRouteId`, `writable`.
- [x] Snippets tab uses the generic table renderer with manifest-declared create/run/delete actions.
- [x] Each route sets `Permission`, `Risk`, `AuditEvent`; passes manifest validation (2.2)

## Definition of done
The real SSH projection renders the SSH tabs in the UI (replacing the SSH fixture), driven entirely by the manifest.

## Notes
Replace `web/fixtures/ssh.json` consumption with the live projection for this connection.
