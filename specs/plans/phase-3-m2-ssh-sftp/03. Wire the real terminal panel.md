# Phase 3 · Step 3 — Wire the real terminal panel

**Milestone:** M2 · **Status:** ☐ Not started
**Depends on:** 3.2, 1.7 · **Spec refs:** [v2 §8.1](../../v2.md), [v2 §12](../../v2.md)

## Goal
Replace the stub `TerminalPanel` with a real xterm.js ↔ `ssh.shell` WS — the first
real streaming validation.

## Checklist
- [ ] xterm.js bound to the `ssh.shell` WS via a ticket (§7.4)
- [ ] PTY resize handshake (cols/rows) on terminal resize
- [ ] Binary framing; backpressure handling
- [ ] Scrollback, copy/paste, theme
- [ ] Reconnect on transient drop; stream held in the `sessions` store (survives tab/pane switch)

## Definition of done
An interactive shell works against the test server; resize is correct; switching tabs and back keeps the live session and scrollback.

## Notes
This is the panel a mock WS could not validate (1.7) — exercise resize/backpressure/reconnect for real.
