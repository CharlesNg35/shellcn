# Phase 3 · Step 4 — Wire the real file browser panel

**Milestone:** M2 · **Status:** ☐ Not started
**Depends on:** 3.2 · **Spec refs:** [v2 §6](../../v2.md), [v2 §13](../../v2.md)

## Goal
Real `FileBrowserPanel` over the SFTP routes.

## Checklist
- [ ] List + navigate directories (paginated for large dirs)
- [ ] Download (streamed) and upload (streamed/multipart)
- [ ] Rename / delete / mkdir; show permissions, size, mtime
- [ ] Inline file view/edit via `CodeEditorPanel` for text files
- [ ] Errors surfaced clearly (perm denied, not found)

## Definition of done
Browse, upload, and download work against the test server through the same SSH session as the terminal (no re-auth).

## Phase 3 exit (M2)
SSH terminal + SFTP work end-to-end through the wrapper (auth, secret decryption, channel lifecycle, audit, manifest rendering). Update [`../README.md`](../README.md) Phase 3 to ✅.
