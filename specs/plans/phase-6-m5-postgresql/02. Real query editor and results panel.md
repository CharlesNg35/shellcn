# Phase 6 · Step 2 — Real query editor and results panel

**Milestone:** M5 · **Status:** ☐ Not started
**Depends on:** 6.1, 1.7 · **Spec refs:** [v2 §12](../../v2.md), [v2 §13](../../v2.md)

## Goal
Replace the stub `QueryEditorPanel` with a real Monaco SQL editor + paginated
results — the fourth streaming/large-data validation.

## Checklist
- [ ] Monaco SQL editor (syntax, basic completion from schema)
- [ ] Execute route: streamed/paginated results (`Page<Row>`), cursor-based
- [ ] Results grid (virtual scroll); column types; NULL rendering
- [ ] Multi-statement handling; per-statement results/tabs
- [ ] Cancel a running query

## Definition of done
Run a query, paginate through results, and cancel a long-running query against a test DB.

## Notes
`Table`/`Data` detail tab also reuses the results grid for browse-table-data.
