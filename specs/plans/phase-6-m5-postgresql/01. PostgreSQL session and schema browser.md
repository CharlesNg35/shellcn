# Phase 6 · Step 1 — PostgreSQL session and schema browser

**Milestone:** M5 · **Status:** ✅ Done — 2026-05-26
**Depends on:** Phase 2 · **Spec refs:** [v2 §8](../../project.md), [v2 §7.2](../../project.md), [v2 §13](../../project.md)

## Goal
A `pgx` session and the lazy schema-browser tree.

## Checklist
- [x] Connect via `cfg.Net.DialContext` (`pgx`); pooling
- [x] Lazy tree: database → schemas → Tables / Views / Functions / Sequences (paginated)
- [x] Per-table routes: columns, indexes, constraints, row count/size
- [x] `HealthCheck` (ping) + `Close`
- [x] Integration proof against a live PostgreSQL test database

## Definition of done
The schema tree loads from a test database; expanding nodes lazily fetches children.

## Notes
This is the connection-level capability (schema browser); query/data live in resource detail.
PostgreSQL is direct TCP only in M5. Agent transport remains reserved for Docker
and Kubernetes-style private control-plane access.
