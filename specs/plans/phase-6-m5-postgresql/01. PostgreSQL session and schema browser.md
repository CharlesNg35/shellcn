# Phase 6 · Step 1 — PostgreSQL session and schema browser

**Milestone:** M5 · **Status:** ☐ Not started
**Depends on:** Phase 2 · **Spec refs:** [v2 §8](../../v2.md), [v2 §7.2](../../v2.md), [v2 §13](../../v2.md)

## Goal
A `pgx` session and the lazy schema-browser tree.

## Checklist
- [ ] Connect via `cfg.Net.DialContext` (`pgx`); pooling
- [ ] Lazy tree: database → schemas → Tables / Views / Functions / Sequences (paginated)
- [ ] Per-table routes: columns, indexes, constraints, row count/size
- [ ] `HealthCheck` (ping) + `Close`

## Definition of done
The schema tree loads from a test database; expanding nodes lazily fetches children.

## Notes
This is the connection-level capability (schema browser); query/data live in resource detail.
