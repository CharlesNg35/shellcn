# Phase 7 · Step 4 — YAML editor and events

**Milestone:** M6 · **Status:** ☐ Not started
**Depends on:** 7.2 · **Spec refs:** [v2 §12](../../v2.md), [v2 §6.3](../../v2.md)

## Goal
Resource YAML editing (the real `CodeEditorPanel` use case) and the events view.

## Checklist
- [ ] Get/Apply resource YAML via `CodeEditorPanel` (CodeMirror): schema validation, diff view
- [ ] Server-side apply with dry-run option; surface validation errors
- [ ] Events table (namespaced + resource-scoped), live-updating
- [ ] Risk: apply/delete tagged appropriately, confirm on destructive

## Definition of done
Edit a Deployment's YAML and apply it (with dry-run + diff); the events table streams cluster events.

## Phase 7 exit (M6)
Kubernetes works over both direct and L7-agent transport — the full plugin model is proven across L4 and L7. Update [`../README.md`](../README.md) Phase 7 to ✅.
